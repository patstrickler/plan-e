{
  "name": "plan-e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 3000 || python -m SimpleHTTPServer 3000 || npx serve .",
    "build": "echo 'No build step needed - static files ready to deploy'",
    "start": "python3 -m http.server 3000 || python -m SimpleHTTPServer 3000 || npx serve ."
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
